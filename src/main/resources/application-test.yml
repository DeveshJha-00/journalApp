jwt:
  secret: ${JWT_SECRET_KEY}

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/testdb

  redis:
    host: localhost
    port: 6379

  main:
    allow-bean-definition-overriding: true