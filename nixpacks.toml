[phases.setup]
nixPkgs = ["openjdk8"]

[phases.install]
cmds = ["chmod +x ./mvnw"]

[phases.build]
cmds = ["./mvnw clean package -DskipTests"]

[start]
cmd = "java -jar target/journalApp-0.0.1-SNAPSHOT.jar"